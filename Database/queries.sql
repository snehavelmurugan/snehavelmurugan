create database car_leasing_system;
use car_leasing_system;
create table userdetail(
	userid long,
    username varchar(50),
    email varchar(50),
    drivinglicense varchar(50),
    password varchar(50)
    );